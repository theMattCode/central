{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "assistant-service",
  "projectType": "application",
  "root": "services/assistant",
  "sourceRoot": "services/assistant/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "services/assistant",
        "command": "cargo clippy --all-targets --all-features -- -D warnings"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "services/assistant",
        "command": "cargo test --all-targets"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "services/assistant",
        "command": "cargo build --release"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "services/assistant",
        "command": "cargo check --all-targets"
      }
    },
    "container-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build --file services/assistant/Dockerfile --tag central/services/assistant:latest ."
      }
    },
    "container-run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker run --rm -i --name central-assistant -p 5020:8080 central/services/assistant:latest"
      }
    }
  },
  "tags": ["scope:platform", "type:service"]
}
