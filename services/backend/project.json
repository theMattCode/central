{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "backend",
  "projectType": "application",
  "root": "services/backend",
  "sourceRoot": "services/backend/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "services/backend",
        "command": "cargo clippy --all-targets --all-features -- -D warnings"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "services/backend",
        "command": "cargo test --all-targets"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "services/backend",
        "command": "cargo build --release"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "services/backend",
        "command": "cargo check --all-targets"
      }
    },
    "container-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build --file services/backend/Dockerfile --tag central/services/backend:latest ."
      }
    },
    "container-run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker run --rm -i --name central-backend -p 5010:8080 central/services/backend:latest"
      }
    }
  },
  "tags": ["scope:platform", "type:service"]
}
