{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "weather-service",
  "projectType": "application",
  "root": "services/weather",
  "sourceRoot": "services/weather/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "services/weather",
        "command": "cargo clippy --all-targets --all-features -- -D warnings"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "services/weather",
        "command": "cargo test --all-targets"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "services/weather",
        "command": "cargo build --release"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "services/weather",
        "command": "cargo check --all-targets"
      }
    },
    "container-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build --file services/weather/Dockerfile --tag central/services/weather:latest ."
      }
    },
    "container-run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker run --rm -i --name central-weather -p 5010:8080 central/services/weather:latest"
      }
    }
  },
  "tags": ["scope:platform", "type:service"]
}
